#include "main.h"
#include <stdio.h>
/**
 * main - prints the number of argument with a new line
 * @argc: input argument counts
 * @argv: strings of input argument
 *
 * Return: Always 0.
 */

int main(int argc, char __attribute__((__unused__)) *argv[])
{
	printf("%d\n", argc);
	return (0);
}
